node_modules/
dist/
out/
.turbo/
*.log

# compiled native addon (node-gyp output)
packages/*-helper/build/

# compiled native addon (napi-rs output). The loader and its types are generated
# from the Rust sources by `napi build`, so only the hand-written index.* is kept.
target/
packages/*-helper/*.node
packages/*-helper/binding.js
packages/*-helper/binding.d.ts

# the bridge script, published into the app by the synthv-script build
apps/voxpane/resources/synthv/

# the bridge script's build identity, stamped by its own build
packages/synthv-script/src/lua/version.ts
