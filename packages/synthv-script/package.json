{
  "name": "@karaoke-v/synthv-script",
  "version": "0.1.0",
  "private": true,
  "description": "The karaoke-v bridge script that runs inside Synthesizer V Studio 2: reads the playhead, notes and view transform, and publishes them to the app.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "pnpm typecheck && tstl -p tsconfig.json && tstl -p tsconfig.smoke.json",
    "deploy": "node scripts/deploy.mjs",
    "dump": "node scripts/dump.mjs"
  },
  "devDependencies": {
    "lua-types": "^2.14.1",
    "typescript-to-lua": "^1.34.0"
  }
}
