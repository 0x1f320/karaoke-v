{
  "name": "@karaoke-v/macos-helper",
  "version": "0.1.0",
  "private": true,
  "description": "Native macOS addon for karaoke-v: sticks an Electron window to a target app's window via the Accessibility API (in-process AXObserver).",
  "main": "index.js",
  "types": "index.d.ts",
  "os": [
    "darwin"
  ],
  "scripts": {
    "build": "./build.sh"
  },
  "dependencies": {
    "node-addon-api": "^8.3.0"
  },
  "devDependencies": {
    "node-gyp": "^11.0.0"
  }
}
