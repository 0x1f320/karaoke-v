{
  "name": "@voxpane/macos-helper",
  "version": "0.1.0",
  "private": true,
  "description": "Native macOS addon for voxpane: sticks an Electron window to a target app's window and reads SynthV's piano-roll geometry via the Accessibility API (in-process AXObserver).",
  "main": "index.js",
  "types": "index.d.ts",
  "os": [
    "darwin"
  ],
  "napi": {
    "binaryName": "macos-helper",
    "targets": [
      "aarch64-apple-darwin",
      "x86_64-apple-darwin"
    ]
  },
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.8.2"
  }
}
